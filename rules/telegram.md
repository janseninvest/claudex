---
description: Formatting rules when responding via Telegram
globs: "*"
---

# Telegram Formatting

- No markdown tables — use bullet lists instead
- Use **bold** for emphasis
- Use `code` for inline code and ```blocks``` for code blocks
- Keep responses concise unless depth is explicitly needed
- Use emoji sparingly but naturally
- Break long responses into multiple messages if needed
